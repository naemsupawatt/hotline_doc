# บันทึกการใช้ AI ช่วยพัฒนา

โจทย์ข้อ 10: "อนุญาตให้ใช้เครื่องมือ AI ช่วยเขียนโค้ดได้เต็มที่ แต่ทีมต้อง
อธิบายโค้ดทุกส่วนที่ส่งได้ และต้องระบุในรายงานว่าใช้ AI ช่วยในส่วนใดบ้าง"

บันทึกทุกครั้งที่ใช้ — ระหว่างทำจะใช้เวลาแค่ 10 วินาที แต่ถ้ารอไปเขียนวันสุดท้ายจะนึกไม่ออก

| วันที่ | ส่วนที่ใช้ AI ช่วย | เครื่องมือ | ใครตรวจทาน | หมายเหตุ |
|---|---|---|---|---|
| 19 ก.ย. 69 | ออกแบบ schema 20 ตาราง (`backend/app/models/`) + migration แรก | Claude Code | _ยังไม่ได้ตรวจทาน_ | โครงสร้างตารางและคำอธิบายเหตุผลในไฟล์ |
| 19 ก.ย. 69 | seed กฎจำแนกประเภท + ค่าธรรมเนียม (`app/seeds/classification.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ค่าตัวเลขมาจากตารางข้อ 4 ของโจทย์ |
| 19 ก.ย. 69 | ชุดทดสอบกฎจำแนก 13 เคส (`backend/tests/`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ครอบ T-01..T-05 + เคสขอบ |
| 19 ก.ย. 69 | ER Diagram + คำอธิบาย 3NF (`docs/database.md`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | deliverable ข้อ 2 |
| 19 ก.ย. 69 | API เข้าสู่ระบบ + หน้า Login (`api/v1/auth.py`, `(public)/login/`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M1 บางส่วน |
| 19 ก.ย. 69 | API สมัครสมาชิก + หน้า Register | Claude Code | _ยังไม่ได้ตรวจทาน_ | ตัด OTP ออกตามมติทีม |
| 19 ก.ย. 69 | ตรวจเลขบัตรประชาชน (`core/thai_id.py`) + เทสต์ 20 เคส | Claude Code | _ยังไม่ได้ตรวจทาน_ | สูตร check digit |
| 19 ก.ย. 69 | รองรับเข้าสู่ระบบด้วยเบอร์โทร (`core/phone.py`, `lib/phone.ts`) + เทสต์ 20 เคส | Claude Code | _ยังไม่ได้ตรวจทาน_ | ปิดช่องว่างที่หน้า login บอกว่ารับเบอร์แต่หน้าสมัครไม่เก็บ |
| 19 ก.ย. 69 | ตัดคอลัมน์ `birth_date` ออก (migration `7f21c0d94e3a`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | โจทย์ไม่ได้ขอ และเป็น PII ที่ไม่มีใครใช้ |
| 19 ก.ย. 69 | สคริปต์สร้าง `docs/schema.sql` จากโมเดล (`backend/scripts/dump_schema.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ไฟล์เดิมค้างอยู่ที่ migration แรก ยังมีคอลัมน์ OTP |
| 19 ก.ย. 69 | แก้บั๊กช่องเลขบัตร/เบอร์โทรเตือนผิดทั้งที่กรอกครบ (`RegisterView.tsx`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | เลิกแทรกขีดระหว่างพิมพ์ (เคอร์เซอร์กระโดดทำตัวเลขสลับ) + `onTouched` + `maxLength` |
| 19 ก.ย. 69 | เปิด CORS ให้ `127.0.0.1:3000` ด้วย (`core/config.py`, `.env.example`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | เบราว์เซอร์ถือว่าคนละ origin กับ `localhost` ถ้าขาดจะสมัครไม่ได้โดยไม่มีสาเหตุที่มองเห็น |
| 19 ก.ย. 69 | seed เอกสาร 6 รายการของเส้น "ไม่เข้าข่ายโรงแรม" + จุดติดต่อ 19 อปท. (`seeds/documents.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | รายการเอกสารมาจากสเปกที่ทีมกำหนด เทสต์ 11 เคสครอบ M3/M4 |
| 19 ก.ย. 69 | service จำแนกประเภท + endpoint wizard (`services/classification.py`, `api/v1/wizard.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M2+M3+M4 ในการเรียกครั้งเดียว เทสต์ 21 เคสครอบ T-01..T-05 |
| 19 ก.ย. 69 | หน้าประเมินที่พัก + รายการเอกสาร 2 หมวด (`(operator)/operator/wizard/`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ตามแบบหน้าจอ `docs/reference-ui/04` และ `01` |
| 19 ก.ย. 69 | เปิดคำขอ + รายการคำขอ + guard ตามบทบาท (`services/application.py`, `api/v1/applications.py`, `api/deps.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M6/M7/M9 เทสต์ 11 เคส รวมฝาแฝดของ T-09 ฝั่งผู้ยื่น |
| 19 ก.ย. 69 | หน้าเริ่มยื่นคำขอ + หน้ารายละเอียดคำขอ (`(operator)/operator/applications/[no]/`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ต่อกับ API ที่มีอยู่ + แยกข้อความ error ตอนยิงไม่ถึงเซิร์ฟเวอร์ |
| 19 ก.ย. 69 | แยกที่อยู่ที่พักเป็นช่องย่อย (migration `847dd66e54db`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | บ้านเลขที่/หมู่/ซอย/ถนน/ตำบล/อำเภอ/จังหวัด/ไปรษณีย์ + เทสต์ 8 เคส |
| 19 ก.ย. 69 | อัปโหลดเอกสาร + ยื่นคำขอ (`services/document.py`, `api/v1/documents.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M5/M6 เก็บเป็นรุ่น + slot หลายไฟล์ + T-06 ระบุเอกสารที่ขาด เทสต์ 13 เคส |
| 19 ก.ย. 69 | หน้าจออัปโหลด + แถบความคืบหน้า + ปุ่มยื่น (`applications/[no]/ApplicationView.tsx`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ต่อกับ API M5/M6 · แก้ `api.ts` ให้ส่ง FormData ได้ |
| 19 ก.ย. 69 | API เจ้าหน้าที่: คิวงาน ตรวจเอกสาร สรุปผล (`services/officer.py`, `api/v1/officer.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M8/M9 + T-08 + T-09 ครบทั้งปฏิเสธและบันทึก เทสต์ 11 เคส |
| 19 ก.ย. 69 | แก้บั๊ก JWT ถูกปฏิเสธแบบสุ่ม (`core/security.py`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | clock skew บน WSL ทำให้ `iat` ล้ำอนาคต ใส่ leeway 30 วินาที |
| 19 ก.ย. 69 | หน้าคิวคำขอ + หน้าตรวจเอกสารของเจ้าหน้าที่ (`(officer)/officer/`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | ตามแบบหน้าจอ `docs/reference-ui/07` + endpoint เปิดไฟล์สำหรับเจ้าหน้าที่ |
| 19 ก.ย. 69 | ออกใบอนุญาต/หนังสือรับรอง + หน้าพิมพ์ (`services/license.py`, `license/LicenseView.tsx`) | Claude Code | _ยังไม่ได้ตรวจทาน_ | M10 สองชนิดในตารางเดียว + snapshot ค่าธรรมเนียม เทสต์ 5 เคส |

> **ค้างตรวจทาน** — ทั้ง 4 รายการข้างบนยังไม่มีใครในทีมอ่านจนอธิบายได้
> จุดที่ต้องเข้าใจให้ได้ก่อนนำเสนอคือ **ทำไมกฎประเภท 1/2 ถึงไม่กำหนด `min_rooms`**
> (คำตอบอยู่ใน `docs/database.md` หัวข้อ "กับดัก T-02") เพราะเป็นจุดที่กรรมการ
> น่าจะถามตรง ๆ และเป็นเหตุผลเชิงออกแบบ ไม่ใช่แค่โค้ด
>
> จุดที่สองคือ **ทำไมเลขบัตรประชาชนถึงไม่เคยออกไปกับ API** — โจทย์ข้อ 8 ถามตรง ๆ
> ว่าข้อมูลใดต้องจำกัดการเข้าถึงเป็นพิเศษ คำตอบของเราอยู่ในคอมเมนต์ที่
> `backend/app/models/user.py` และ `docs/database.md` หัวข้อ "ข้อมูลส่วนบุคคล"
>
> จุดที่สามคือ **ทำไมเบอร์โทรถึงต้องเก็บเป็นตัวเลขล้วน** — เพราะเบอร์เป็นกุญแจ
> เข้าสู่ระบบอีกทางหนึ่งตาม M1 ถ้าเก็บตามที่ผู้ใช้พิมพ์ คนที่สมัครด้วย
> "081-234-5678" แล้วล็อกอินด้วย "0812345678" จะกลายเป็นคนละคนทันที
> ตรรกะอยู่ที่ `core/phone.py` และทำซ้ำฝั่งหน้าเว็บที่ `lib/phone.ts`

## สิ่งที่ต้องตอบให้ได้ตอนนำเสนอ

กรรมการมีสิทธิ์ชี้ไปที่โค้ดบรรทัดไหนก็ได้แล้วถามว่าทำงานอย่างไร
ก่อน commit โค้ดที่ AI เขียน ให้คนที่เป็นเจ้าของส่วนนั้นอ่านจนอธิบายได้ก่อน
