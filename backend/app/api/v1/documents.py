"""M5 — อัปโหลดเอกสาร + ตรวจชนิด/ขนาดไฟล์ + เก็บเป็นรุ่น (version)

ข้อ 8 ของโจทย์ถามตรง ๆ ว่า "อัปโหลดฉบับเดิมซ้ำเพื่อแก้ไข ควรทับไฟล์เดิม
หรือเก็บเป็นรุ่นใหม่" -> ระบบนี้เลือกเก็บเป็นรุ่นใหม่ทุกครั้ง เพราะไฟล์ที่
เจ้าหน้าที่เคยตรวจต้องย้อนดูได้ และ AuditLog ต้องชี้ไปยังไฟล์รุ่นที่ถูกตรวจจริง

เจ้าของงานส่วนนี้: <ใส่ชื่อสมาชิก>
"""

from fastapi import APIRouter

router = APIRouter()

# TODO M5: POST   /{application_id}/upload   รับ JPG/PNG/PDF, จำกัด MAX_UPLOAD_MB
# TODO M5: GET    /{application_id}          สถานะรายฉบับ (ครบ/ขาด)
# TODO M5: DELETE /file/{file_id}            ลบเฉพาะรุ่นล่าสุดที่ยังไม่ถูกตรวจ
# TODO S1: POST   /file/{file_id}/precheck   ตรวจเบื้องต้น (เบลอ/อ่านไม่ออก) T-07
