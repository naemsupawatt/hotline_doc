"""M6, M7, M10 — คำขอ: สร้าง / ยื่น / ติดตามสถานะ / ใบอนุญาต

เจ้าของงานส่วนนี้: <ใส่ชื่อสมาชิก>
"""

from fastapi import APIRouter

router = APIRouter()

# TODO M6:  POST /                  สร้างคำขอจากผลจำแนกของ wizard
# TODO M6:  POST /{id}/submit       ยื่น -- ต้องบล็อกถ้าเอกสารบังคับไม่ครบ (T-06)
#                                   และออกเลขที่คำขอให้อ้างอิง
# TODO M7:  GET  /{id}/status       ขั้นตอนปัจจุบัน + ผู้รับผิดชอบ + รออยู่กี่วัน
# TODO M9:  GET  /{id}/audit        ประวัติการเปลี่ยนสถานะทั้งหมด
# TODO M10: GET  /{id}/license      ใบอนุญาตอิเล็กทรอนิกส์ + เลขอ้างอิง
