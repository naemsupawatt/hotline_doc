"""M11 — รายงานภาพรวมสำหรับหน่วยงานส่วนกลาง

ส่วนกลาง "ดูภาพรวมโดยไม่ก้าวก่ายการพิจารณารายคำขอ" -> endpoint กลุ่มนี้
ต้องคืนข้อมูลสรุปเท่านั้น ห้ามคืนข้อมูลที่ระบุตัวผู้ยื่นรายบุคคล

เจ้าของงานส่วนนี้: <ใส่ชื่อสมาชิก>
"""

from fastapi import APIRouter

router = APIRouter()

# TODO M11: GET /summary        จำนวนคำขอแยกตามประเภทและตามท้องถิ่น
# TODO M11: GET /bottlenecks    คำขอค้างอยู่ที่ขั้นตอนใดบ้าง
# TODO S5:  GET /wait-times     ระยะเวลารอเฉลี่ยในแต่ละขั้นตอน
# TODO S6:  GET /workload       ภาระงานเจ้าหน้าที่แบบไม่ระบุตัวตน (ใช้เลขอ้างอิง)
