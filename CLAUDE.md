# HoTLinE Doc — บริบทโปรเจกต์

ระบบยื่นขอใบอนุญาตประกอบธุรกิจโรงแรมและที่พักแรมแบบออนไลน์ จังหวัดภูเก็ต
โจทย์ Hackathon ที่ 2 ชุดวิชา 977-121 วิทยาลัยการคอมพิวเตอร์ ม.อ. ภูเก็ต

**โจทย์ฉบับเต็มอยู่ที่ [`docs/brief/Hackathon_HoTLinE_Doc.pdf`](docs/brief/Hackathon_HoTLinE_Doc.pdf)** — อ่านก่อนตัดสินใจอะไรที่กระทบขอบเขตงาน
**แบบหน้าจอที่ทีมต้องการอยู่ที่ [`docs/reference-ui/`](docs/reference-ui/) 8 ภาพ** — ใช้เป็นต้นแบบสไตล์
**เหตุผลเบื้องหลังการตัดสินใจทุกข้ออยู่ที่ [`docs/decisions.md`](docs/decisions.md)**

## ข้อจำกัดที่กำหนดทุกอย่าง

- **เวลา 12 ชั่วโมง** (เสาร์–อาทิตย์ 19–20 ก.ย. 2569 วันละ 6 ชม.) เขียนโค้ดใหม่ในวันงานเท่านั้น
- **ทีม 3 คน ใช้คอมพิวเตอร์เครื่องเดียว** — ทำงานแบบ 2 คนที่เครื่อง (driver + navigator) + 1 คนทำเอกสาร สลับทุก 90 นาที
- โจทย์เขียนไว้ตรง ๆ ว่า *"ระบบที่ทำครบ 5 ฟังก์ชันและใช้งานได้จริง ได้คะแนนมากกว่าระบบที่ทำ 15 ฟังก์ชันแต่กดแล้วไม่ทำงาน"*
  → **ทำ Must Have (M1–M11) ให้ทำงานได้จริงก่อนเสมอ** อย่าเผลอไปทำ Should/Could Have

## สภาพแวดล้อม (WSL Ubuntu 22.04)

| | | |
|---|---|---|
| Frontend | Next.js 16 + TypeScript + Tailwind v4 | :3000 |
| Backend | FastAPI + SQLAlchemy 2 + Alembic | :8000 |
| Database | PostgreSQL 14 (รันอยู่แล้วในเครื่อง) | :5432 |

```bash
nvm use            # จำเป็น! default ของเครื่องเป็น Node 12 ซึ่ง Next 16 ใช้ไม่ได้
./scripts/dev.sh   # เปิดทั้งสองฝั่ง, Ctrl+C ปิดทั้งคู่
```

**กับดักของเครื่องนี้ที่ต้องรู้:**
- `python3` ของระบบคือ 3.10 ซึ่งไม่มี `StrEnum` — โปรเจกต์ใช้ **Python 3.13 ผ่าน uv** เสมอ (`uv run ...`) อย่าเรียก `python3` ตรง ๆ
- Node ที่ถูกต้องคือ 24 ผ่าน nvm ตาม `.nvmrc` — ถ้าลืม `nvm use` จะเจอ error แปลก ๆ ที่ไม่เกี่ยวกับโค้ด
- role ฐานข้อมูลคือ `naemsupawat` (superuser) ไม่ใช่ `postgres` — ดู `backend/.env.example`

## กฎที่ทีมตกลงกัน (ห้ามละเมิดโดยไม่ปรึกษา)

1. **ห้าม hard-code กฎเกณฑ์** — เงื่อนไขจำแนกประเภท ค่าธรรมเนียม รายการเอกสาร และ อปท. 19 แห่ง
   ต้องอ่านจากฐานข้อมูล เพราะโจทย์บังคับว่า Super Admin ต้องแก้ได้เองโดยไม่แก้โค้ด (US-09)
   นี่คือสิ่งที่กรรมการน่าจะขอให้สาธิต และผูกกับเกณฑ์ข้อ 2 (25 คะแนน)
2. **สีอยู่ที่ `frontend/src/app/globals.css` ที่เดียว** ห้ามเขียน `#xxxxxx` ในคอมโพเนนต์
3. **สถานะ → สี/ข้อความ อยู่ที่ `StatusPill.tsx` ที่เดียว** (7 สถานะเอกสาร, 7 สถานะคำขอ)
   ค่าต้องตรงกับ `backend/app/models/enums.py` เสมอ
4. **เวลาประทับจากฐานข้อมูลเสมอ** (`server_default=func.now()`) ห้ามให้ผู้ใช้กรอกเวลาเอง — NFR ข้อ Audit Trail
5. **อัปโหลดไฟล์ซ้ำ = สร้างรุ่นใหม่ ไม่ทับของเดิม** เพราะต้องย้อนดูไฟล์ที่เจ้าหน้าที่เคยตรวจได้
6. **ข้อความ error ต้องเป็นภาษาที่ผู้ใช้ทั่วไปเข้าใจ** ไม่ใช่ error code ดิบ — NFR ข้อ Usability
7. **ห้ามใช้ข้อมูลส่วนบุคคลจริง** ทุกชนิด ใช้ข้อมูลจำลองเท่านั้น (กติกาข้อ 14)
8. **`git push` ทุกครั้งที่สลับคนจับคีย์บอร์ด** — เครื่องมีเครื่องเดียว push คือ backup เดียวที่มี
   และตอบเกณฑ์ "commit สม่ำเสมอ ไม่ใช่ commit ครั้งเดียวตอนจบ" ไปในตัว

## กับดักที่เสียคะแนนง่ายที่สุด

- **T-02**: เงื่อนไข "ไม่เข้าข่ายโรงแรม" คือ ห้อง ≤ 8 **และ** คน ≤ 30 (AND ทั้งคู่)
  ที่พัก 8 ห้องแต่รับ 36 คน ต้องตอบว่า **เข้าข่ายต้องขอใบอนุญาต** — ถ้าเช็กแต่จำนวนห้องจะตอบผิดทันที
- **T-09**: เจ้าหน้าที่เปิดคำขอข้ามเขตต้องถูกปฏิเสธ **และบันทึกความพยายามนั้นลง AuditLog ด้วย**
- **M6**: ต้องบล็อกการยื่นเมื่อเอกสารบังคับไม่ครบ และระบุให้ชัดว่าขาดฉบับใดบ้าง

## เกณฑ์ให้คะแนน (ใช้ตัดสินลำดับความสำคัญ)

| เกณฑ์ | คะแนน |
|---|---|
| ตอบโจทย์ความต้องการผู้ใช้ (requirement, user story, ใช้ง่าย) | 30 |
| คุณภาพการออกแบบระบบและฐานข้อมูล (ER, 3NF, API, แยก FE/BE) | 25 |
| ความสมบูรณ์และใช้งานได้จริงตอนเดโม | 25 |
| การนำเสนอและการทำงานเป็นทีม (ทุกคนต้องพูดและอธิบายส่วนตัวเองได้) | 20 |

## สถานะปัจจุบัน

**เสร็จแล้ว:** โครงโปรเจกต์ทั้งสองฝั่ง, design token จากสีโลโก้จริง, คอมโพเนนต์กลาง 6 ตัว
(`PageHeader` `SectionCard` `DocRow` `StatusPill` `Stepper` `AiNotice`), `<Logo>` `<Mascot>`,
brand asset แปลงเป็น WebP แล้ว, enums สถานะทั้งระบบ, router 7 กลุ่ม (ยังเป็น stub),
seed อปท. 19 แห่ง, สคริปต์ setup/dev/git-setup, โครงเอกสารส่งมอบ

**ยังไม่เริ่ม:** ER Diagram + models + migration (คอขวดถัดไป), endpoint ทุกตัว,
หน้าจอจริงทุกหน้า, เอกสารส่งมอบข้อ 1/2/3/5/6

**ขั้นถัดไปที่วางไว้:** ออกแบบ ER Diagram + SQL schema ให้ `ClassificationRule` / `FeeSchedule` /
`DocumentFile` (versioning) เป็น config-driven ตั้งแต่แรก

## โครงสร้างโค้ด

```
backend/app/
  core/       config (อ่าน .env) / db / security (bcrypt + JWT)
  models/     SQLAlchemy models + enums.py (สถานะทั้งระบบ)
  schemas/    Pydantic → กลายเป็น /docs อัตโนมัติ ใส่ examples= ทุกครั้ง
  api/v1/     auth wizard applications documents officer reports admin
  services/   business logic — ห้ามเขียนปนใน endpoint
  seeds/      ข้อมูลตั้งต้น
frontend/src/
  app/(public|operator|officer|central|admin)/   แยกตามบทบาท แต่ละกลุ่มมี README
  components/brand|common|ui
  lib/api.ts  fetch ที่เดียวของทั้งแอป ห้าม fetch ตรงในคอมโพเนนต์
  types/enums.ts  ต้องตรงกับ backend/app/models/enums.py
```

## หมายเหตุเรื่องการใช้ AI

โจทย์ข้อ 10 อนุญาตให้ใช้ AI ช่วยเขียนโค้ดได้เต็มที่ **แต่บังคับว่าทีมต้องอธิบายโค้ดทุกส่วนที่ส่งได้
และต้องระบุในรายงานว่าใช้ AI ช่วยส่วนใดบ้าง** → บันทึกลง `docs/ai-usage.md` ทุกครั้งที่ทำงานเสร็จหนึ่งก้อน
และก่อน commit ให้เจ้าของส่วนนั้นอ่านจนอธิบายได้ก่อน
