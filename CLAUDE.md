# HoTLinE Doc — บริบทโปรเจกต์

ระบบยื่นขอใบอนุญาตประกอบธุรกิจโรงแรมและที่พักแรมแบบออนไลน์ จังหวัดภูเก็ต
โจทย์ Hackathon ที่ 2 ชุดวิชา 977-121 วิทยาลัยการคอมพิวเตอร์ ม.อ. ภูเก็ต

**โจทย์ฉบับเต็มอยู่ที่ [`docs/brief/Hackathon_HoTLinE_Doc.pdf`](docs/brief/Hackathon_HoTLinE_Doc.pdf)** — อ่านก่อนตัดสินใจอะไรที่กระทบขอบเขตงาน
**แบบหน้าจอที่ทีมต้องการอยู่ที่ [`docs/reference-ui/`](docs/reference-ui/) 8 ภาพ** — ใช้เป็นต้นแบบสไตล์
**เหตุผลเบื้องหลังการตัดสินใจทุกข้ออยู่ที่ [`docs/decisions.md`](docs/decisions.md)**

## ข้อจำกัดที่กำหนดทุกอย่าง

- **เวลา 12 ชั่วโมง** (เสาร์–อาทิตย์ 19–20 ก.ย. 2569 วันละ 6 ชม.) เขียนโค้ดใหม่ในวันงานเท่านั้น
- **ทีม 3 คน ใช้คอมพิวเตอร์เครื่องเดียว** — ทำงานแบบ 2 คนที่เครื่อง (driver + navigator) + 1 คนทำเอกสาร สลับทุก 90 นาที
- โจทย์เขียนไว้ตรง ๆ ว่า *"ระบบที่ทำครบ 5 ฟังก์ชันและใช้งานได้จริง ได้คะแนนมากกว่าระบบที่ทำ 15 ฟังก์ชันแต่กดแล้วไม่ทำงาน"*
  → **ทำ Must Have (M1–M11) ให้ทำงานได้จริงก่อนเสมอ** อย่าเผลอไปทำ Should/Could Have

## สภาพแวดล้อม (WSL Ubuntu 22.04)

| | | |
|---|---|---|
| Frontend | Next.js 16 + TypeScript + Tailwind v4 | :3000 |
| Backend | FastAPI + SQLAlchemy 2 + Alembic | :8000 |
| Database | PostgreSQL 14 (รันอยู่แล้วในเครื่อง) | :5432 |

```bash
nvm use            # จำเป็น! default ของเครื่องเป็น Node 12 ซึ่ง Next 16 ใช้ไม่ได้
./scripts/dev.sh   # เปิดทั้งสองฝั่ง, Ctrl+C ปิดทั้งคู่
```

**กับดักของเครื่องนี้ที่ต้องรู้:**
- `python3` ของระบบคือ 3.10 ซึ่งไม่มี `StrEnum` — โปรเจกต์ใช้ **Python 3.13 ผ่าน uv** เสมอ (`uv run ...`) อย่าเรียก `python3` ตรง ๆ
- Node ที่ถูกต้องคือ 24 ผ่าน nvm ตาม `.nvmrc` — ถ้าลืม `nvm use` จะเจอ error แปลก ๆ ที่ไม่เกี่ยวกับโค้ด
- role ฐานข้อมูลคือ `naemsupawat` (superuser) ไม่ใช่ `postgres` — ดู `backend/.env.example`

## กฎที่ทีมตกลงกัน (ห้ามละเมิดโดยไม่ปรึกษา)

1. **ห้าม hard-code กฎเกณฑ์** — เงื่อนไขจำแนกประเภท ค่าธรรมเนียม รายการเอกสาร และ อปท. 19 แห่ง
   ต้องอ่านจากฐานข้อมูล เพราะโจทย์บังคับว่า Super Admin ต้องแก้ได้เองโดยไม่แก้โค้ด (US-09)
   นี่คือสิ่งที่กรรมการน่าจะขอให้สาธิต และผูกกับเกณฑ์ข้อ 2 (25 คะแนน)
2. **สีอยู่ที่ `frontend/src/app/globals.css` ที่เดียว** ห้ามเขียน `#xxxxxx` ในคอมโพเนนต์
3. **สถานะ → สี/ข้อความ อยู่ที่ `StatusPill.tsx` ที่เดียว** (7 สถานะเอกสาร, 7 สถานะคำขอ)
   ค่าต้องตรงกับ `backend/app/models/enums.py` เสมอ
4. **เวลาประทับจากฐานข้อมูลเสมอ** (`server_default=func.now()`) ห้ามให้ผู้ใช้กรอกเวลาเอง — NFR ข้อ Audit Trail
5. **อัปโหลดไฟล์ซ้ำ = สร้างรุ่นใหม่ ไม่ทับของเดิม** เพราะต้องย้อนดูไฟล์ที่เจ้าหน้าที่เคยตรวจได้
6. **ข้อความ error ต้องเป็นภาษาที่ผู้ใช้ทั่วไปเข้าใจ** ไม่ใช่ error code ดิบ — NFR ข้อ Usability
7. **ห้ามใช้ข้อมูลส่วนบุคคลจริง** ทุกชนิด ใช้ข้อมูลจำลองเท่านั้น (กติกาข้อ 14)
8. **`git push` ทุกครั้งที่สลับคนจับคีย์บอร์ด** — เครื่องมีเครื่องเดียว push คือ backup เดียวที่มี
   และตอบเกณฑ์ "commit สม่ำเสมอ ไม่ใช่ commit ครั้งเดียวตอนจบ" ไปในตัว

## กับดักที่เสียคะแนนง่ายที่สุด

- **T-02**: เงื่อนไข "ไม่เข้าข่ายโรงแรม" คือ ห้อง ≤ 8 **และ** คน ≤ 30 (AND ทั้งคู่)
  ที่พัก 8 ห้องแต่รับ 36 คน ต้องตอบว่า **เข้าข่ายต้องขอใบอนุญาต** — ถ้าเช็กแต่จำนวนห้องจะตอบผิดทันที
- **T-09**: เจ้าหน้าที่เปิดคำขอข้ามเขตต้องถูกปฏิเสธ **และบันทึกความพยายามนั้นลง AuditLog ด้วย**
- **M6**: ต้องบล็อกการยื่นเมื่อเอกสารบังคับไม่ครบ และระบุให้ชัดว่าขาดฉบับใดบ้าง

## เกณฑ์ให้คะแนน (ใช้ตัดสินลำดับความสำคัญ)

| เกณฑ์ | คะแนน |
|---|---|
| ตอบโจทย์ความต้องการผู้ใช้ (requirement, user story, ใช้ง่าย) | 30 |
| คุณภาพการออกแบบระบบและฐานข้อมูล (ER, 3NF, API, แยก FE/BE) | 25 |
| ความสมบูรณ์และใช้งานได้จริงตอนเดโม | 25 |
| การนำเสนอและการทำงานเป็นทีม (ทุกคนต้องพูดและอธิบายส่วนตัวเองได้) | 20 |

## สถานะปัจจุบัน (อัปเดต 20 ก.ย. 2569)

### Must Have ครบ 11 ข้อ ใช้งานได้จริงทั้ง API และหน้าจอ

| | สถานะ |
|---|---|
| M1 สมัคร/เข้าสู่ระบบ (อีเมลหรือเบอร์โทร) | เสร็จ |
| M2 wizard จำแนกประเภท + เหตุผล | เสร็จ |
| M3 เอกสาร 2 หมวด | เสร็จ ครบทั้ง 3 ประเภท |
| M4 จุดติดต่อหน่วยงานครบ 19 อปท. | เสร็จ |
| M5 อัปโหลด + ตรวจชนิด/ขนาด + เก็บเป็นรุ่น | เสร็จ |
| M6 ยื่นคำขอ + บล็อกเมื่อเอกสารไม่ครบ | เสร็จ |
| M7 ติดตามสถานะ + รายการคำขอของฉัน | เสร็จ |
| M8 หน้าเจ้าหน้าที่ ตรวจ/สรุปผล | เสร็จ |
| M9 audit trail ทุกการเปลี่ยนสถานะ | เสร็จ |
| M10 ออกใบอนุญาต/หนังสือรับรอง พิมพ์ได้ | เสร็จ |
| M11 ภาพรวมส่วนกลาง | เสร็จ |
| US-09 Super Admin แก้เกณฑ์เอง | เสร็จ (กฎ + ค่าธรรมเนียม) |
| ผู้ช่วยตอบคำถาม (แชทบอท ยังไม่ต่อ AI) | เสร็จ |
| อีเมลแจ้งเตือนเมื่อเจ้าหน้าที่ตัดสิน | เสร็จ |

**เทสต์ backend 194 เคส** (`cd backend && uv run pytest`) รันชุดเต็มหลายรอบไม่ล้ม

**หน้าจอทั้งหมด:** `/login` `/register` `/operator/wizard` `/operator/applications`
`/operator/applications/[no]` `/operator/applications/[no]/license`
`/operator/applications/[no]/forms/[code]`
`/officer/queue` `/officer/applications/[no]` `/officer/applications/[no]/forms/[code]`
`/officer/applications/[no]/license`
`/central/overview` `/admin/settings`
ทุกกลุ่มบทบาทมี `AppNav` ร่วมกัน เมนูเปลี่ยนตามบทบาท

### การตัดสินใจเชิงออกแบบที่ต้องรู้ก่อนแก้อะไร

- **ตัด OTP ออก** (มติทีม) กลไกกันบัญชีขยะที่เหลือคือ **หนึ่งเลขบัตรต่อหนึ่งบัญชี
  และเลขต้องผ่านหลักตรวจสอบ** มีเทสต์ `test_duplicate_national_id_is_the_remaining_bot_guard`
  เป็นหลักฐาน ถ้าจะเอากลับ ให้ดู git history
- **ไม่เก็บวันเดือนปีเกิด** (migration `7f21c0d94e3a`) เพราะโจทย์ไม่ได้ขอ
- **เบอร์โทรเก็บเป็นตัวเลขล้วนเสมอ** (`core/phone.py` + `lib/phone.ts` แก้คู่กัน)
- **ห้ามใส่ `national_id` ดิบใน response schema ใด ๆ** ใช้ `national_id_masked`
- **ที่อยู่แยกเป็นช่องย่อย** และเก็บอำเภอ/จังหวัดไว้ที่ `property` เอง ไม่ derive
  จาก อปท. เพราะที่อยู่บนเอกสารเป็นข้อเท็จจริง ณ วันยื่น (และ อบจ. มี
  `district = "ทั้งจังหวัด"` ซึ่งไม่ใช่ชื่ออำเภอจริง)
- **`slot_no` ≠ `version_no`** slot = คนละไฟล์ (ภาพหลายมุม), version = ไฟล์เดิมส่งใหม่
- **`display_order` อยู่ที่ `document_requirement` ไม่ใช่ `document_type`** เพราะเอกสาร
  ฉบับเดียวกันถูกใช้หลายประเภทแต่เรียงคนละลำดับ ลำดับเป็นคุณสมบัติของรายการ (3NF)
- **`document_type.parent_id`** ใช้บอกว่าเอกสารเป็นช่องแนบที่อยู่ในแบบฟอร์มอีกฉบับ
  (A07–A09 อยู่ใต้แบบ ร.ร.1) หน้าจอแสดงซ้อน ไม่ใช่ลอยเป็นรายการแยก
- **ช่องแนบใน ร.ร.1 ไม่บังคับ** เพราะบางฉบับไม่มีจริงตามรูปแบบกิจการ
  (บุคคลธรรมดาไม่มีหนังสือรับรองนิติบุคคล) ถ้าบังคับจะบล็อกการยื่นโดยไม่มีทางแก้
- **แบบฟอร์มที่ระบบกรอกให้มีสองฉบับ ใช้กลไกเดียวกัน** A01 หนังสือแจ้งฯ (ไม่เข้าข่ายโรงแรม)
  และ A06 ร.ร.1 (ประเภท 1/2) — endpoint เดียว `GET /applications/{no}/forms/{code}`
  service เดียว `system_form.py` โครงกระดาษเดียว `SystemFormPaper.tsx`
  ต่างกันแค่เนื้อความใน `FormView.tsx`
- **แผนที่จุดติดต่อเก็บในฐานข้อมูล** `local_authority.map_url` / `contact_point.map_url`
  (migration `c4a91d5e77b2`) ค่าตั้งต้น 19 แห่งอยู่ที่ `seeds/local_authorities.py`
  **ห้ามเขียนลิงก์ลงในคอมโพเนนต์** การแปลงลิงก์เป็น URL สำหรับฝังอยู่ที่ `lib/maps.ts`
  ที่เดียว ฝังแผนที่โดยไม่ใช้ API key จึงต้องมีลิงก์เปิด Google Maps คู่กันเสมอ
  (decisions ข้อ 17)
- **เจ้าหน้าที่เปิดดูเอกสารที่ออกให้ได้แล้ว** `/officer/applications/[no]/license`
  ปุ่มอยู่ในกล่อง "ออกเอกสารแล้ว" จึงขึ้นเฉพาะหลังลงนามออกเอกสาร ไม่ใช่แค่อนุมัติ
  ใช้ `LicenseView` และ `presenters.to_license_out_for` ร่วมกับฝั่งผู้ยื่น (decisions ข้อ 16)
- **เจ้าหน้าที่เปิดดูแบบฟอร์มได้แล้ว** `/officer/applications/[no]/forms/[code]` แสดง
  หนังสือทั้งใบพร้อมลายมือชื่อ (อ่านอย่างเดียว) เพราะไฟล์แนบของแบบฟอร์มคือรูปลายเซ็น
  อย่างเดียว เปิดดูแล้วไม่รู้ว่าเซ็นกำกับอะไร กระดาษใบเดียวกันทั้งสองฝั่ง ประกอบที่
  `presenters.to_system_form_out` ที่เดียว หน้าจอใช้ `SystemFormView` ร่วมกัน
  (prop `viewer`) **ห้ามแยกเป็นสองชุด** (decisions ข้อ 16)
- **ลายมือชื่อเก็บเป็นไฟล์แนบของแบบฟอร์มนั้นเอง** ไม่ใช่คอลัมน์ใหม่ จึงได้ระบบรุ่น
  การตรวจของเจ้าหน้าที่ และการล็อกหลังยื่น มาโดยไม่ต้องเขียนเพิ่ม
  **PDF ออกด้วยการพิมพ์ของเบราว์เซอร์ ไม่มีไลบรารี PDF ฝั่งเซิร์ฟเวอร์** (decisions ข้อ 12)
  **ลายมือชื่อเป็นเอกสารบังคับ** แบบฟอร์มที่ยังไม่เซ็นบล็อกการยื่น (M6)
  นับรวมใน `missing_mandatory` และใน `pending_documents` ของเจ้าหน้าที่ด้วย
- **ช่องติ๊กเอกสารแนบใน ร.ร.1 ระบบติ๊กให้จากไฟล์ที่แนบแล้ว** ไม่มีคอลัมน์ "ติ๊กแล้ว" (3NF)
- **ชื่อผู้ยื่นบนแบบฟอร์มแก้ได้** `PATCH /applications/{no}/applicant` แก้ที่ตาราง
  `operator` (display_name / is_juristic / juristic_reg_no) เลขทะเบียนนิติบุคคล
  เก็บเป็นตัวเลขล้วน 13 หลักเหมือนเบอร์โทร
- **อีเมลแจ้งเตือนส่งหลัง commit ด้วย BackgroundTasks** (decisions ข้อ 15)
  ส่งเฉพาะตอนเจ้าหน้าที่ตัดสิน (ตีกลับ/อนุมัติ/ไม่อนุมัติ/ออกเอกสาร) ไปยังอีเมลที่ใช้สมัคร
  **ห้ามให้การส่งเมลทำให้การตัดสินล้มเหลว** — กลืน exception แล้วบันทึก AuditLog
  `notification.email` แทน (success / failed / skipped) มีเทสต์คุมไว้
  ค่าเริ่มต้นในโค้ดคือ `EMAIL_BACKEND=outbox` เขียนไฟล์ลง `storage/outbox/` ไม่ส่งออกจริง
  **เครื่องทีมตั้ง `.env` เป็น `smtp` ผ่าน Gmail แล้ว** (`hotlinedoc.info@gmail.com`)
  ต้องใช้ **app password 16 หลัก** เท่านั้น รหัสผ่านบัญชีปกติได้ `535 BadCredentials`
  ตรวจค่าที่ตั้งด้วย `uv run python -m scripts.send_test_email ปลายทาง@example.com`
  **เทสต์ถูกบังคับให้ใช้ outbox เสมอที่ `tests/conftest.py`** ห้ามถอดออก ไม่งั้นรันเทสต์
  ทีหนึ่งจะยิงเมลจริงไปหาที่อยู่สมมติหลายสิบฉบับ (ดู decisions ข้อ 15)
  **`EMAIL_REDIRECT_TO` = โหมดทดสอบ** ดึงอีเมลถึงผู้ยื่นทุกฉบับมาเข้ากล่องเดียว
  ตอนนี้ตั้งไว้ที่ `naemsupawat@gmail.com` **ต้องลบออกจาก `.env` ก่อนใช้งานจริง**
  ไม่ฝังที่อยู่ในโค้ดเพราะโค้ดขึ้น git (กติกาข้อ 7) conftest บังคับปิดโหมดนี้ในเทสต์
  ข้อความอีเมลอยู่ที่ `TEMPLATES` ใน `services/notification.py` ที่เดียว
- **แชทบอทเป็นชุดคำถามที่เตรียมไว้ + จับคำสำคัญ ยังไม่ต่อ AI** (decisions ข้อ 14)
  คำถาม-คำตอบอยู่ที่ `lib/chatbot.ts` ที่เดียว **ห้ามเขียนกฎเกณฑ์ลงในคำตอบ**
  (เอกสาร/ค่าธรรมเนียม/ชนิดไฟล์/ขนาดไฟล์/อปท.) ต้องดึงจาก API เสมอ
  ถ้าจับคำไม่ได้ให้บอกว่าตอบไม่ได้ ห้ามเดา
- **เจ้าหน้าที่ต้องลงนามตอนออกเอกสาร** `issue-license` รับไฟล์ PNG เสมอ
  เก็บที่ `license.issuer_signature_path` (migration `5b1e70c4a9d2`) และไปปรากฏ
  บนหน้าพิมพ์ใบอนุญาต ใบที่ออกก่อนหน้านี้ไม่มี หน้าพิมพ์จะเว้นเส้นไว้ (decisions ข้อ 13)
- **`/wizard/classify` ไม่เขียนอะไรลง DB** เป็นการคำนวณล้วน ผลบันทึกตอนกดยื่นเท่านั้น
- **ผลจำแนกเป็น snapshot** ที่ `application_classification` แก้กฎทีหลังไม่กระทบคำขอเก่า
- **ใบอนุญาต snapshot ค่าธรรมเนียม** ทั้ง `fee_schedule_id` และ `fee_amount`
- **แก้อัตราค่าธรรมเนียม = เพิ่มแถวใหม่ ไม่ทับของเดิม** (ปิด `effective_to` แถวเก่า)
- **ตาราง `license` เก็บสองชนิด** แยกด้วย `kind`: `license` (ประเภท 1/2 มีค่าธรรมเนียม
  5 ปี) และ `notice_receipt` (ไม่เข้าข่าย ไม่มีค่าธรรมเนียม ไม่หมดอายุ)
- **คิวเจ้าหน้าที่แบ่ง 4 แท็บตามว่าลูกบอลอยู่ในมือใคร** — ดู
  `frontend/src/app/(officer)/README.md`

### กับดักที่แก้ไปแล้ว อย่าให้กลับมา

- **JWT ถูกปฏิเสธแบบสุ่ม** (`ImmatureSignatureError`) PyJWT ตัดเศษวินาทีตอนสร้าง `iat`
  แต่เทียบเวลาแบบทศนิยมตอนตรวจ นาฬิกา WSL ขยับถอยนิดเดียวก็พัง
  แก้ด้วย `leeway` ใน `core/security.py` **ห้ามถอดออก**
- **คิวเจ้าหน้าที่เคยกรองด้วย `OPEN_STATUSES`** ทำให้คำขอหายจากคิวทันทีที่อนุมัติ
  แล้วกดออกใบอนุญาตไม่ได้อีก — ต้องใช้ `NEEDS_OFFICER_ACTION` (รวม `approved`)
  ส่วน `OPEN_STATUSES` ใช้กันการตัดสินซ้ำเท่านั้น
- **alembic autogenerate ใส่ `postgresql_nulls_not_distinct`** (PG 15+) บนเครื่อง PG 14
  → ลบ kwarg นี้ทิ้งทุกครั้ง ไม่งั้น downgrade พัง
- **เทสต์ที่ใช้อีเมลชุดเดียวกันแล้วลบระหว่างเคส** ทำให้ล้มแบบสุ่ม
  ให้แต่ละเคสมีผู้ใช้ของตัวเอง
- **ช่องกรอกที่ใส่ขีดให้ระหว่างพิมพ์** ทำให้เคอร์เซอร์กระโดดจนตัวเลขสลับ
  เลขบัตร/เบอร์โทรจึงรับตัวเลขล้วน แล้วแสดงแบบมีขีดใต้ช่องแทน
- **CORS ต้องมีทั้ง `localhost:3000` และ `127.0.0.1:3000`** เบราว์เซอร์ถือว่าคนละ origin

### ยังไม่ทำ

- **deliverable ที่ยังว่าง** — `docs/requirements.md` (0 bytes, เกณฑ์ข้อ 1 = 30 คะแนน),
  `docs/test-cases.md` (0 bytes แต่มีเทสต์อัตโนมัติ 152 เคสครอบ T-01..T-09 แล้ว
  ยกมาเรียงเป็นตารางได้), `docs/wireframes/` (มีหน้าจอจริง 10 หน้าแล้ว แคปได้),
  `docs/presentation/` สไลด์ 7 นาที
  ชื่อเจ้าของอยู่ที่ `operator.display_name` / `is_juristic` / `juristic_reg_no`
  ซึ่งยังไม่มีหน้าจอให้แก้
- admin: หน้าแก้รายการเอกสาร / จุดติดต่อ อปท. / จัดการผู้ใช้
- ใบอนุญาตที่ออกก่อน migration `5b1e70c4a9d2` ไม่มีลายมือชื่อผู้ลงนามเก็บไว้
  และคำขอที่ยื่นก่อนกฎ "ต้องเซ็นก่อนยื่น" จะอนุมัติไม่ได้จนกว่าจะตีกลับให้เซ็นใหม่
- Should Have (S1–S7) และ Could Have (C1–C4) ทั้งหมด
- **`.env.example` มีรหัสผ่านฐานข้อมูลจริงอยู่** (`hotline`) ควรเปลี่ยนเป็นค่าตัวอย่าง
- **หน้า "คำขอของฉัน" ของผู้ประกอบการยังใช้ช่องกรองสถานะ** ต่างจากหน้าคิวเจ้าหน้าที่
  ที่เปลี่ยนเป็นแท็บแล้ว ยังไม่ได้ทำให้เหมือนกัน
- **ล้างคำขอทดสอบทั้งหมดได้ด้วย `cd backend && uv run python -m scripts.reset_applications --yes`**
  (ไม่ใส่ `--yes` = แสดงว่าจะลบอะไรบ้างโดยยังไม่ลบ) ลบคำขอ ผลจำแนก ประวัติสถานะ
  ไฟล์แนบทั้งแถวและบนดิสก์ ผลตรวจ ใบอนุญาต ที่พัก และ audit ของสิ่งเหล่านั้น
  **ไม่ลบ** บัญชีผู้ใช้และข้อมูลตั้งต้น (กฎ ค่าธรรมเนียม เอกสาร อปท.)
  ตอนนี้ฐานข้อมูลว่างคำขอแล้ว หน้า `/central/overview` จึงเป็นศูนย์ทั้งหมด
  ถ้าจะเดโมให้มีตัวเลข ต้องสร้างคำขอตัวอย่างใหม่ก่อน

## โครงสร้างโค้ด

```
backend/app/
  core/       config (อ่าน .env) / db / security (bcrypt + JWT)
  models/     SQLAlchemy models + enums.py (สถานะทั้งระบบ)
  schemas/    Pydantic → กลายเป็น /docs อัตโนมัติ ใส่ examples= ทุกครั้ง
  api/v1/     auth wizard applications documents officer reports admin
  services/   business logic — ห้ามเขียนปนใน endpoint
  seeds/      ข้อมูลตั้งต้น
frontend/src/
  app/(public|operator|officer|central|admin)/   แยกตามบทบาท แต่ละกลุ่มมี README
  components/brand|common|ui
  lib/api.ts  fetch ที่เดียวของทั้งแอป ห้าม fetch ตรงในคอมโพเนนต์
  types/enums.ts  ต้องตรงกับ backend/app/models/enums.py
```

## หมายเหตุเรื่องการใช้ AI

โจทย์ข้อ 10 อนุญาตให้ใช้ AI ช่วยเขียนโค้ดได้เต็มที่ **แต่บังคับว่าทีมต้องอธิบายโค้ดทุกส่วนที่ส่งได้
และต้องระบุในรายงานว่าใช้ AI ช่วยส่วนใดบ้าง** → บันทึกลง `docs/ai-usage.md` ทุกครั้งที่ทำงานเสร็จหนึ่งก้อน
และก่อน commit ให้เจ้าของส่วนนั้นอ่านจนอธิบายได้ก่อน
