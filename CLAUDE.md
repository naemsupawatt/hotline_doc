# HoTLinE Doc — บริบทโปรเจกต์

ระบบยื่นขอใบอนุญาตประกอบธุรกิจโรงแรมและที่พักแรมแบบออนไลน์ จังหวัดภูเก็ต
โจทย์ Hackathon ที่ 2 ชุดวิชา 977-121 วิทยาลัยการคอมพิวเตอร์ ม.อ. ภูเก็ต

**โจทย์ฉบับเต็มอยู่ที่ [`docs/brief/Hackathon_HoTLinE_Doc.pdf`](docs/brief/Hackathon_HoTLinE_Doc.pdf)** — อ่านก่อนตัดสินใจอะไรที่กระทบขอบเขตงาน
**แบบหน้าจอที่ทีมต้องการอยู่ที่ [`docs/reference-ui/`](docs/reference-ui/) 8 ภาพ** — ใช้เป็นต้นแบบสไตล์
**เหตุผลเบื้องหลังการตัดสินใจทุกข้ออยู่ที่ [`docs/decisions.md`](docs/decisions.md)**

## ข้อจำกัดที่กำหนดทุกอย่าง

- **เวลา 12 ชั่วโมง** (เสาร์–อาทิตย์ 19–20 ก.ย. 2569 วันละ 6 ชม.) เขียนโค้ดใหม่ในวันงานเท่านั้น
- **ทีม 3 คน ใช้คอมพิวเตอร์เครื่องเดียว** — ทำงานแบบ 2 คนที่เครื่อง (driver + navigator) + 1 คนทำเอกสาร สลับทุก 90 นาที
- โจทย์เขียนไว้ตรง ๆ ว่า *"ระบบที่ทำครบ 5 ฟังก์ชันและใช้งานได้จริง ได้คะแนนมากกว่าระบบที่ทำ 15 ฟังก์ชันแต่กดแล้วไม่ทำงาน"*
  → **ทำ Must Have (M1–M11) ให้ทำงานได้จริงก่อนเสมอ** อย่าเผลอไปทำ Should/Could Have

## สภาพแวดล้อม (WSL Ubuntu 22.04)

| | | |
|---|---|---|
| Frontend | Next.js 16 + TypeScript + Tailwind v4 | :3000 |
| Backend | FastAPI + SQLAlchemy 2 + Alembic | :8000 |
| Database | PostgreSQL 14 (รันอยู่แล้วในเครื่อง) | :5432 |

```bash
nvm use            # จำเป็น! default ของเครื่องเป็น Node 12 ซึ่ง Next 16 ใช้ไม่ได้
./scripts/dev.sh   # เปิดทั้งสองฝั่ง, Ctrl+C ปิดทั้งคู่
```

**กับดักของเครื่องนี้ที่ต้องรู้:**
- `python3` ของระบบคือ 3.10 ซึ่งไม่มี `StrEnum` — โปรเจกต์ใช้ **Python 3.13 ผ่าน uv** เสมอ (`uv run ...`) อย่าเรียก `python3` ตรง ๆ
- Node ที่ถูกต้องคือ 24 ผ่าน nvm ตาม `.nvmrc` — ถ้าลืม `nvm use` จะเจอ error แปลก ๆ ที่ไม่เกี่ยวกับโค้ด
- role ฐานข้อมูลคือ `naemsupawat` (superuser) ไม่ใช่ `postgres` — ดู `backend/.env.example`

## กฎที่ทีมตกลงกัน (ห้ามละเมิดโดยไม่ปรึกษา)

1. **ห้าม hard-code กฎเกณฑ์** — เงื่อนไขจำแนกประเภท ค่าธรรมเนียม รายการเอกสาร และ อปท. 19 แห่ง
   ต้องอ่านจากฐานข้อมูล เพราะโจทย์บังคับว่า Super Admin ต้องแก้ได้เองโดยไม่แก้โค้ด (US-09)
   นี่คือสิ่งที่กรรมการน่าจะขอให้สาธิต และผูกกับเกณฑ์ข้อ 2 (25 คะแนน)
2. **สีอยู่ที่ `frontend/src/app/globals.css` ที่เดียว** ห้ามเขียน `#xxxxxx` ในคอมโพเนนต์
3. **สถานะ → สี/ข้อความ อยู่ที่ `StatusPill.tsx` ที่เดียว** (7 สถานะเอกสาร, 7 สถานะคำขอ)
   ค่าต้องตรงกับ `backend/app/models/enums.py` เสมอ
4. **เวลาประทับจากฐานข้อมูลเสมอ** (`server_default=func.now()`) ห้ามให้ผู้ใช้กรอกเวลาเอง — NFR ข้อ Audit Trail
5. **อัปโหลดไฟล์ซ้ำ = สร้างรุ่นใหม่ ไม่ทับของเดิม** เพราะต้องย้อนดูไฟล์ที่เจ้าหน้าที่เคยตรวจได้
6. **ข้อความ error ต้องเป็นภาษาที่ผู้ใช้ทั่วไปเข้าใจ** ไม่ใช่ error code ดิบ — NFR ข้อ Usability
7. **ห้ามใช้ข้อมูลส่วนบุคคลจริง** ทุกชนิด ใช้ข้อมูลจำลองเท่านั้น (กติกาข้อ 14)
8. **`git push` ทุกครั้งที่สลับคนจับคีย์บอร์ด** — เครื่องมีเครื่องเดียว push คือ backup เดียวที่มี
   และตอบเกณฑ์ "commit สม่ำเสมอ ไม่ใช่ commit ครั้งเดียวตอนจบ" ไปในตัว

## กับดักที่เสียคะแนนง่ายที่สุด

- **T-02**: เงื่อนไข "ไม่เข้าข่ายโรงแรม" คือ ห้อง ≤ 8 **และ** คน ≤ 30 (AND ทั้งคู่)
  ที่พัก 8 ห้องแต่รับ 36 คน ต้องตอบว่า **เข้าข่ายต้องขอใบอนุญาต** — ถ้าเช็กแต่จำนวนห้องจะตอบผิดทันที
- **T-09**: เจ้าหน้าที่เปิดคำขอข้ามเขตต้องถูกปฏิเสธ **และบันทึกความพยายามนั้นลง AuditLog ด้วย**
- **M6**: ต้องบล็อกการยื่นเมื่อเอกสารบังคับไม่ครบ และระบุให้ชัดว่าขาดฉบับใดบ้าง

## เกณฑ์ให้คะแนน (ใช้ตัดสินลำดับความสำคัญ)

| เกณฑ์ | คะแนน |
|---|---|
| ตอบโจทย์ความต้องการผู้ใช้ (requirement, user story, ใช้ง่าย) | 30 |
| คุณภาพการออกแบบระบบและฐานข้อมูล (ER, 3NF, API, แยก FE/BE) | 25 |
| ความสมบูรณ์และใช้งานได้จริงตอนเดโม | 25 |
| การนำเสนอและการทำงานเป็นทีม (ทุกคนต้องพูดและอธิบายส่วนตัวเองได้) | 20 |

## สถานะปัจจุบัน

**เสร็จแล้ว:** โครงโปรเจกต์ทั้งสองฝั่ง, design token จากสีโลโก้จริง, คอมโพเนนต์กลาง 6 ตัว
(`PageHeader` `SectionCard` `DocRow` `StatusPill` `Stepper` `AiNotice`), `<Logo>` `<Mascot>`,
brand asset แปลงเป็น WebP แล้ว, enums สถานะทั้งระบบ, router 7 กลุ่ม (ยังเป็น stub),
สคริปต์ setup/dev/git-setup, โครงเอกสารส่งมอบ

**ชั้นข้อมูลเสร็จแล้ว (19 ก.ย. บ่าย):**
- schema 20 ตาราง + migration แรก รันผ่านทั้ง upgrade และ downgrade
- `ClassificationRule` / `FeeSchedule` / `DocumentFile` เป็น config-driven ตั้งแต่แรกตามที่วางไว้
- seed ลง DB จริงแล้ว: อปท. 19 แห่ง, ประเภทที่พัก 4, กฎจำแนก 4, อัตราค่าธรรมเนียม 2
- `backend/tests/` 13 เคสครอบ T-01..T-05 + เคสขอบ ผ่านหมด (`uv run pytest`)
- ER Diagram + คำอธิบาย 3NF อยู่ที่ `docs/database.md` (deliverable ข้อ 2 เสร็จ)

**M1 เสร็จแล้ว (19 ก.ย. เย็น):**
- `POST /auth/register` `/auth/login` `GET /auth/me` ใช้งานได้จริง ทดสอบครบ
- หน้า `/login` และ `/register` (สมัครเสร็จเข้าใช้งานได้ทันที) `/` redirect ไป `/login`
- หน้าตรวจสอบระบบดีไซน์ย้ายไป `/scaffold`
- เลขบัตรประชาชน: ตรวจ check digit ทั้งสองฝั่ง เก็บ unique แสดงแบบปิดบังเสมอ
  **ห้ามเพิ่ม `national_id` ดิบลงใน response schema ใด ๆ**
- เข้าสู่ระบบได้ทั้งอีเมลและเบอร์โทรตาม M1 — เบอร์เก็บเป็น**ตัวเลขล้วน**เสมอ
  (`core/phone.py` + `lib/phone.ts` ต้องแก้คู่กัน) หนึ่งเบอร์ต่อหนึ่งบัญชี
- ไม่เก็บวันเดือนปีเกิด ตัดออกแล้ว (migration `7f21c0d94e3a`) เพราะโจทย์ไม่ได้ขอ
  และเป็นข้อมูลส่วนบุคคลที่ไม่มีส่วนไหนของระบบใช้
- บัญชีสาธิต 4 บทบาท รหัส `demo1234`
- เทสต์ backend 53 เคส (`uv run pytest`)
- `docs/schema.sql` สร้างใหม่ด้วย `cd backend && uv run python -m scripts.dump_schema > ../docs/schema.sql`
  ทุกครั้งที่ schema เปลี่ยน (เป็น deliverable ข้อ 2 ห้ามปล่อยให้ค้าง)

**ตัด OTP ออกแล้ว (ทีมตัดสินใจ 19 ก.ย.)** — M1 เขียนว่าต้องมี "กลไกยืนยันตัวตน
เพื่อแยกผู้ใช้จริงออกจากบัญชีขยะ" ถ้ากรรมการถามข้อนี้ ให้ตอบว่ากลไกที่เหลืออยู่คือ
**หนึ่งเลขบัตรประชาชนต่อหนึ่งบัญชี และเลขต้องผ่านหลักตรวจสอบ** จึงสร้างบัญชีจำนวนมาก
ด้วยเลขมั่ว ๆ ไม่ได้ (มีเทสต์ `test_duplicate_national_id_is_the_remaining_bot_guard`
เป็นหลักฐาน) ถ้าจะเอา OTP กลับ ให้ `alembic downgrade` หนึ่งขั้นแล้วดู git history

**M5–M9 เสร็จแล้ว (19 ก.ย. ค่ำ):**
- อัปโหลดเอกสาร: ตรวจชนิด/ขนาดจาก DB, อัปซ้ำ = `version_no` ใหม่, หลายไฟล์ = `slot_no` ใหม่
- ยื่นคำขอ: บล็อกเมื่อเอกสารบังคับไม่ครบ **และระบุว่าขาดฉบับใด** (T-06)
- เจ้าหน้าที่: คิวเฉพาะเขตตน, ตรวจรายฉบับ, สรุปผล, กันอนุมัติข้ามขั้น
- **T-09 ครบทั้งสองครึ่ง** ปฏิเสธ 403 + บันทึก AuditLog ทุกเส้นทาง
- **T-08 วนครบวง** ขอแก้ไข → ผู้ยื่นส่งใหม่เป็นรุ่นที่ 2 → ตรวจ → อนุมัติ
- หน้าจอครบ: `/operator/wizard` `/operator/applications/[no]` `/officer/queue` `/officer/applications/[no]`
- เทสต์ backend 129 เคส รันชุดเต็ม 10 รอบไม่ล้ม

**กับดักที่แก้ไปแล้ว อย่าให้กลับมา:**
- JWT ถูกปฏิเสธแบบสุ่มด้วย `ImmatureSignatureError` — PyJWT ตัดเศษวินาทีตอนสร้าง `iat`
  แต่เทียบเวลาแบบทศนิยมตอนตรวจ นาฬิกา WSL ขยับถอยนิดเดียวก็พัง
  แก้ด้วย `leeway` ใน `core/security.py` **ห้ามถอดออก**
- alembic autogenerate ใส่ `postgresql_nulls_not_distinct` (PG 15+) บนเครื่อง PG 14 → ลบทิ้งทุกครั้ง
- เทสต์ที่ใช้อีเมลชุดเดียวกันแล้วลบระหว่างเคส ทำให้ล้มแบบสุ่ม — ให้แต่ละเคสมีผู้ใช้ของตัวเอง

**M10/M11 เสร็จแล้ว (19 ก.ย. ค่ำ) — Must Have ครบ 11 ข้อ:**
- M10 ออกเอกสารได้สองชนิดในตาราง `license` เดียว แยกด้วย `kind`
  `license` (ประเภท 1/2 มีค่าธรรมเนียม 5 ปี) / `notice_receipt` (ไม่เข้าข่าย ไม่มีค่าธรรมเนียม ไม่หมดอายุ)
  snapshot ทั้ง `fee_schedule_id` และ `fee_amount` ตามข้อควรคิดข้อ 1 ของโจทย์ข้อ 8
  หน้าพิมพ์ใช้ `window.print()` + คลาส `print:` ไม่ได้สร้าง PDF ฝั่งเซิร์ฟเวอร์
- M11 `GET /reports/overview` (central + super_admin) คืนเฉพาะตัวเลขรวม
  ไม่มีเลขที่คำขอ ชื่อผู้ยื่น หรือชื่อเจ้าหน้าที่ — มีเทสต์ยืนยันว่าไม่หลุด
- `AppNav` แถบนำทางร่วม เมนูเปลี่ยนตามบทบาท ใส่เฉพาะหน้าที่สร้างแล้วจริง
- เทสต์ backend 137 เคส

**หน้าจอที่มีตอนนี้:** `/login` `/register` `/operator/wizard`
`/operator/applications` `/operator/applications/[no]` `/operator/applications/[no]/license`
`/officer/queue` `/officer/applications/[no]` `/central/overview`

**US-09 เสร็จแล้ว:** `/admin/settings` แก้เกณฑ์จำแนกและอัตราค่าธรรมเนียมจากหน้าจอ
พร้อมปุ่ม "ลองจำแนก" ที่ทดสอบได้ทันทีโดยไม่บันทึกอะไร — วิธีสาธิตอยู่ใน
`frontend/src/app/(admin)/README.md` **แก้อัตราค่าธรรมเนียม = เพิ่มแถวใหม่
ไม่ทับของเดิม** เพราะใบอนุญาตชี้กลับมาที่ `fee_schedule_id`

**ยังไม่เริ่ม:** admin หน้าแก้รายการเอกสาร/จุดติดต่อ/ผู้ใช้, Should/Could Have ทั้งหมด,
deliverable ข้อ 1 (requirements.md) ข้อ 3 (wireframes) ข้อ 5 (test-cases.md) ข้อ 6 (สไลด์)
(ยังเป็น stub), หน้าจอของผู้ประกอบการ/เจ้าหน้าที่/ส่วนกลางทั้งหมด,
seed เอกสาร A01–A08/B01–B07, เอกสารส่งมอบข้อ 1/3/5/6

**ขั้นถัดไป:** seed `DocumentType` + `DocumentRequirement` (M3/M4 ใช้ตารางนี้ทั้งคู่)
แล้วทำ service จำแนกประเภท + endpoint wizard ให้ต่อกับ FE ได้

**M2/M3/M4 เสร็จแล้ว (19 ก.ย. เย็น) — เฉพาะเส้น "ไม่เข้าข่ายโรงแรม":**
- `GET /wizard/local-authorities` + `POST /wizard/classify` (ไม่ต้องล็อกอิน ตาม US-01)
  คืนประเภท + **เหตุผล** + `matched_rule_code` + ค่าธรรมเนียม + เอกสารแยก 2 หมวด
- `/classify` **ไม่เขียนอะไรลง DB** เป็นการคำนวณล้วน — ผลบันทึกตอนกดยื่นคำขอเท่านั้น
- seed เอกสาร 6 รายการของ `not_hotel` (A01–A05, B01) + จุดติดต่อกองช่างครบ 19 อปท.
- `document_type.is_system_form` = กรอกในระบบ (A01), `allows_multiple` = แนบหลายไฟล์ (A04),
  `accepted_mime` = ชนิดไฟล์ที่รับ ทั้งสามอ่านจาก DB ให้ Super Admin แก้ได้
- `document_file.slot_no` แยกจาก `version_no` — slot = คนละไฟล์, version = ไฟล์เดิมส่งใหม่
- หน้า `/operator/wizard` ใช้งานได้จริง ต่อ API แล้ว
- เทสต์ backend 85 เคส

**ประเภท 1/2 ยังไม่มีรายการเอกสาร** — `classify` ตอบประเภทถูกและคิดค่าธรรมเนียมได้
แต่ `documents` จะว่าง จนกว่าจะเติมใน `app/seeds/documents.py` (ไฟล์เดียว ไม่ต้องแตะโค้ดอื่น)

**กับดัก alembic บนเครื่องนี้:** autogenerate ชอบใส่ `postgresql_nulls_not_distinct`
ซึ่งเป็นไวยากรณ์ PG 15+ แต่เครื่องเป็น PG 14 → downgrade พัง **ให้ลบ kwarg นี้ทุกครั้ง**

## โครงสร้างโค้ด

```
backend/app/
  core/       config (อ่าน .env) / db / security (bcrypt + JWT)
  models/     SQLAlchemy models + enums.py (สถานะทั้งระบบ)
  schemas/    Pydantic → กลายเป็น /docs อัตโนมัติ ใส่ examples= ทุกครั้ง
  api/v1/     auth wizard applications documents officer reports admin
  services/   business logic — ห้ามเขียนปนใน endpoint
  seeds/      ข้อมูลตั้งต้น
frontend/src/
  app/(public|operator|officer|central|admin)/   แยกตามบทบาท แต่ละกลุ่มมี README
  components/brand|common|ui
  lib/api.ts  fetch ที่เดียวของทั้งแอป ห้าม fetch ตรงในคอมโพเนนต์
  types/enums.ts  ต้องตรงกับ backend/app/models/enums.py
```

## หมายเหตุเรื่องการใช้ AI

โจทย์ข้อ 10 อนุญาตให้ใช้ AI ช่วยเขียนโค้ดได้เต็มที่ **แต่บังคับว่าทีมต้องอธิบายโค้ดทุกส่วนที่ส่งได้
และต้องระบุในรายงานว่าใช้ AI ช่วยส่วนใดบ้าง** → บันทึกลง `docs/ai-usage.md` ทุกครั้งที่ทำงานเสร็จหนึ่งก้อน
และก่อน commit ให้เจ้าของส่วนนั้นอ่านจนอธิบายได้ก่อน
